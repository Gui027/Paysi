{
  "name": "paysi",
  "private": true,
  "workspaces": ["web-painel", "web-checkout", "web-site"],
  "scripts": {
    "dev:painel": "npm run dev --workspace web-painel",
    "dev:checkout": "npm run dev --workspace web-checkout",
    "dev:site": "npm run dev --workspace web-site",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspace web-painel"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
